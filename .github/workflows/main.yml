name: CI/CD
on: # rebuild any PRs and main branch changes
  pull_request:
  push:
    branches:
      - master
      - "releases/*"

jobs:
  ci:
    name: Integration
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: npm install
      - run: npm run ci

  cd:
    name: Deployment
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: manusa/actions-publish-docker@master
        name: Build & publish Docker image
        with:
          name: 47ng/actions-clever-cloud
          username: ${{ secrets.DOCKER_HUB_USER }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}
          tag script: |
            return context.ref
              .replace('refs/heads/')
              .replace('refs/tags/')
              .replace('refs/pull/')
              .replace(/\//g, '_')
              .replace(/#/g, '');

# if (ref.match(/[a-f0-9]{40}/)) {
#   // Full Git SHA-1, slice to first 8 chars
#   return ref.slice(0, 8);
# }
# return ref;
